function computeTripledAreaOfARectangle(length, width) {
  const area = 3 * (length * width);
  return area;
}


/**function computeTripledAreaOfARectangle(length, width) {
  const area = length * width;
  return area*3;
} **/
